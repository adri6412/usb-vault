image sdcard.img {
    hdimage {
        partition-table-type = "gpt"
    }

    partition boot {
        image = "boot.vfat"
        size = 256M
    }

    partition rootfs {
        image = "rootfs.ext2"
        size = 120M
    }
}